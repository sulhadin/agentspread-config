# Releases this repo's groups and rolls them out to every consumer. Run it from the release branch:
# Actions → release → Run workflow.
name: release
on: workflow_dispatch
jobs:
  release:
    uses: sulhadin/agentspread/.github/workflows/release.yml@v1.0.0
    secrets: inherit
