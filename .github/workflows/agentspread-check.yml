# Lints groups/ and, on pull requests, checks the conventional-commit title releases are versioned from.
name: check
on:
  pull_request:
    types: [opened, edited, synchronize, reopened]
  push:
jobs:
  check:
    uses: sulhadin/agentspread/.github/workflows/check.yml@v1.1.0
